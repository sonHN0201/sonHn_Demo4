# Some sample Serenity configuration options

#
# WebDriver configuration
#
webdriver {
  driver = chrome
  driver = firefox
  autodownload = true
}
headless.mode = false

serenity {
  test.root = starter
  compress.filenames = false
//  take.screenshots = FOR_FAILURES
}
environments {
    default {
            base.url = "http://practice.automationtesting.in/"
            }
          }
#
# Chrome options can be defined using the chrome.switches property
#
chrome.switches = """--start-maximized;--test-type;--no-sandbox;--ignore-certificate-errors;
                   --disable-popup-blocking;--disable-default-apps;--disable-extensions-file-access-check;
                   --disable-infobars,--disable-gpu"""
chrome_preferences.download.default_directory = "${user.dir}${file.separator}src${file.separator}test${file.separator}resources${file.separator}download"


gecko.firefox.options="""
{
    "start-maximized"
    "args": ["-headless", "-profile", "/path/to/my/profile"],
    "prefs": {
        "dom.ipc.processCount": 8
    },
    "log": {
        "level": "trace"
    }
}
"""
# -Dfirefox.preferences = "browser.download.folderList = 2; browser.download.manager.showWhenStarting = false; browser.download.dir = c: \ Download"